<?php
require_once("connexion_db.php");
$req = $bdd->prepare('select distinct id_conversation from chat_conversation where id_membre=?');
$req->execute(array($_POST['id_user']));
while ($data = $req->fetch())
	{
		echo "<li class='conv' value='".$data['id_conversation']."'><small>Conversation ".$data['id_conversation']."</small> ".'<img src="'.$baseUrl.'public/images/delete.gif" onclick="supprimer_conv('.$data['id_conversation'].')" />'."</li>";
	}
